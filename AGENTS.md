# apisdkgen

Shared Go API SDK generator used by caic and mddb. Keep project-specific CLI entry points in the consumer repositories. Run `make fix`, `make verify`, and `make test` after changes. Do not import either consumer from this module; specifications and output paths belong to consumers.
